[package]
name = "uroboros_rs"
version = "0.1.0"
edition = "2021"

[lib]
name = "uroboros_rs"
path = "src/lib.rs"
crate-type = ["staticlib"]

[profile.release]
panic = "abort"

[profile.dev]
panic = "abort"

[dependencies]
